<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <span class="label label-info ">Nombre</span><br><br>

    <%= f.text_field :username, :required => true %><br><br>
  </div>
  <div class="field">
    <span class="label label-info ">Email</span><br><br>

    <%= f.text_field :email %>
 
</div>
      <div class="field">
    <span class="label label-info ">Admin</span><br><br>

    <%= f.check_box :admin %>
 
</div>
    
  
  <div class="actions">
    <%= f.submit "Enviar",:class =>"btn btn-mini", :disable_with => "Enviando..." %>
  </div>
<% end %>
